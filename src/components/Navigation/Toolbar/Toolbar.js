import React from 'react';

import './Toolbar.css';
import Logo from '../../Logo/Logo';

const toolbar = () => (
    <header className="Toolbar">
        <Logo height="80%"/>
    </header>
);

export default toolbar;